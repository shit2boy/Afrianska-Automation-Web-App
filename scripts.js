const menu= ()=>{
        const closebar = document.getElementById('menuItems');
       if (closebar.style.display === 'none') {
        closebar.style.display = 'block';
       } else {
        closebar.style.display = 'none'
       };

     }